info:
  name: Ingest
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/sop/ingest"
  body:
    type: json
    data: |-
      {
        "overwrite": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
