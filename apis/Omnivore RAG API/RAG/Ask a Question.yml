info:
  name: Ask a Question
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/ask"
  body:
    type: json
    data: |-
      {
        "question": "How does the Bulk Edit Access window work in Banner General?",
        "top_k": 3,
        "module_filter": "general",
        "version_filter": "9.3.37.2",
        "year_filter": "2026"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
