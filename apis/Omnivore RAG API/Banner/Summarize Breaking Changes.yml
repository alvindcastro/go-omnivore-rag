info:
  name: Summarize Breaking Changes
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/banner/summarize/breaking"
  body:
    type: json
    data: |-
      {
        "version": "9.3.22",
        "module": "Finance",
        "top_k": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
