info:
  name: Blob List
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/banner/blob/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
