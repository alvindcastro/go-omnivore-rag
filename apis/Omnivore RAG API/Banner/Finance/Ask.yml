info:
  name: Ask
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/banner/finance/ask"
  body:
    type: json
    data: |-
      {
        "question": "How do I close a fiscal year in Banner Finance?",
        "top_k": 5,
        "version_filter": "",
        "year_filter": "",
        "section_filter": "",
        "source_type": "",
        "mode": "auto"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
