info:
  name: Lookup
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/banner/student/lookup"
  body:
    type: json
    data: |-
      {
        "term": "Course Catalog",
        "top_k": 5,
        "module_filter": "Student",
        "version_filter": "",
        "section_filter": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
