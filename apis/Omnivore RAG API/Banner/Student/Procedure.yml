info:
  name: Procedure
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/banner/student/procedure"
  body:
    type: json
    data: |-
      {
        "topic": "How do I create a CRN for a course section?",
        "top_k": 7,
        "module_filter": "Student",
        "version_filter": "",
        "section_filter": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
