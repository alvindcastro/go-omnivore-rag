info:
  name: Ask
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/banner/hr/ask"
  body:
    type: json
    data: |-
      {
        "question": "How do I create a new position in Banner HR?",
        "top_k": 5,
        "version_filter": "",
        "year_filter": "",
        "section_filter": "",
        "source_type": "",
        "mode": "auto"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
