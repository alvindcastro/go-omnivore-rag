info:
  name: Ask
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/banner/general/ask"
  body:
    type: json
    data: |-
      {
        "question": "What are the prerequisites for upgrading Banner to 9.3.37.2?",
        "top_k": 5,
        "version_filter": "9.3.37.2",
        "year_filter": "",
        "section_filter": "",
        "source_type": "",
        "mode": "auto"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
