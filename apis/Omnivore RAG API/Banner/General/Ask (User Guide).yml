info:
  name: Ask (User Guide)
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/banner/general/ask"
  body:
    type: json
    data: |-
      {
        "question": "How do I navigate the Banner main menu?",
        "top_k": 5,
        "source_type": "banner_user_guide"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
