info:
  name: Index Stats
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/index/stats"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
