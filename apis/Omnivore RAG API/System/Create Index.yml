info:
  name: Create Index
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/index/create"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
